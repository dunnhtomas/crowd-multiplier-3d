using UnityEngine;

namespace CrowdMultiplier.Production
{
    /// <summary>
    /// Minimal production manager for local gameplay
    /// </summary>
    public class ProductionDeploymentManager : MonoBehaviour
    {
        // Empty minimal implementation for local gameplay
        private void Start()
        {
            Debug.Log("Local game mode - minimal production setup");
        }
    }
}
